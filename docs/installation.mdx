---
title: 📦 Installation
description: Per-client setup guides for Claude Code, Claude Desktop, Cursor, VSCode, JetBrains, and more.
---

## 📦 Installation Options

> **Release candidate status:** `@daxiong888/mcp-jenkins@3.0.0-rc.1` is published. Use `@next` for release-candidate evaluation and pin `@3.0.0-rc.1` for a reviewed deployment.

**Option 1: Via npm (recommended)** — no installation needed, just use `npx` in your MCP config:

```bash
npx --yes @daxiong888/mcp-jenkins@next
```

**Option 2: Local installation:**

```bash
git clone https://github.com/daxiong888/mcp-jenkins.git
cd mcp-jenkins
npm install
npm run build
```

## ✅ Release candidate validation

For `3.0.0-rc.1`, the release source passed `npm test` (23 files, 244 tests),
`npm run typecheck`, and `npm run build` locally on macOS arm64 with Node.js
v24.11.1. The published `@next` tarball matched npm's recorded SHA-1, and a
clean registry install completed an MCP stdio `initialize` handshake as
`3.0.0-rc.1`, listed 38 tools, and kept the test URL and credentials out of
stderr.

Run the full disposable Jenkins compatibility matrix with:

```bash
npm run test:integration:docker:matrix
```

The matrix has passed for digest-pinned Jenkins 2.411, 2.477, and 2.568.2. Each
version boots two local controllers and validates their reported versions,
explicit instance routing, credential isolation, and the covered write paths.
Replay is unit/mock tested but excluded from Docker because the stock controller
images lack the required Pipeline/Workflow plugins.

[GitHub Actions run 33042439411](https://github.com/daxiong888/mcp-jenkins/actions/runs/33042439411)
passed `npm ci`, typecheck, build, and all tests on Node.js 20, 22, and 24. No
write request was sent to a real Jenkins instance, and the local controllers do
not reproduce real plugin sets, security policies, SSO, or reverse-proxy
behaviour.

Because `3.0.0-rc.1` is the package's first published version, npm also points
`latest` at it. npm requires the `latest` dist-tag, so it cannot be removed.
This is currently the package's only published version; a future stable `3.0.0`
release will move `latest`, while `next` remains the release-candidate channel.

> **Note:** Keep credentials off the command line entirely. `--api-token` / `--bearer-token` CLI arguments are visible in process lists, and any interactive command carrying a token — including `--env KEY=value` or `VAR=value` prefixes — lands in shell history. Put credentials in your MCP client's `env` config (a local config file — restrict its permissions and never commit it) or inject them from a secret manager. The command-line examples below therefore show only non-secret values.

## 🧭 Choose your environment

- [Claude Code CLI](#claude-code-cli) - Command line interface
- [Claude Code Web](#claude-code-web) - Browser-based Claude
- [Claude Desktop](#claude-desktop) - Desktop application
- [VSCode](#vscode) - With Cline, Claude Dev, Continue
- [Cursor](#cursor) - AI-first IDE
- [Windsurf](#windsurf) - AI-powered code editor
- [GitHub Copilot](#github-copilot) - In VSCode or standalone
- [GitHub Copilot CLI](#github-copilot-cli) - Terminal tool
- [JetBrains IDEs](#all-jetbrains-ides) - IntelliJ, WebStorm, PyCharm, GoLand, etc.

## 🎯 Claude Code CLI

**For:** Claude AI via command line interface

**Via npm with env vars (recommended):**

```bash
claude mcp add --transport stdio --scope user jenkins \
  --env MCP_JENKINS_URL=https://pipeline.yourcompany.com \
  -- npx --yes @daxiong888/mcp-jenkins@latest
```

**Via npm with a CLI URL arg:**

```bash
claude mcp add --transport stdio --scope user jenkins \
  -- npx --yes @daxiong888/mcp-jenkins@latest \
  --url https://pipeline.yourcompany.com
```

**Local installation:**

```bash
claude mcp add --transport stdio --scope user jenkins \
  --env MCP_JENKINS_URL=https://pipeline.yourcompany.com \
  -- node $HOME/path/to/mcp-jenkins/dist/index.js
```

**Multiple Jenkins instances:**

```bash
claude mcp add --transport stdio --scope user jenkins \
  --env MCP_JENKINS_URL="https://pipeline.yourcompany.com,https://scheduler.yourcompany.com" \
  -- npx --yes @daxiong888/mcp-jenkins@latest
# Instances named "pipeline" and "scheduler" automatically from hostnames
```

These commands carry no credentials: after registering, add `MCP_JENKINS_USER`/`MCP_JENKINS_API_TOKEN` (or `MCP_JENKINS_BEARER_TOKEN`) to the generated client config's `env` block — see the JSON examples below — or inject them from a secret manager.

Verify: `claude mcp list` should show `jenkins`

## 🌐 Claude Code Web

**For:** Claude.ai in your browser

**⚠️ Not supported.** The Claude.ai web interface doesn't support custom MCP servers yet.

**Alternatives:**

- Use desktop clients (see below)
- Use CLI tools (see above)
- Test via MCP Inspector: `npm run inspect:dev` → `http://localhost:5173`

## 🖥️ Claude Desktop

**For:** Claude AI desktop application

### 1. Open Configuration File

**macOS:**

```bash
open ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

**Windows:**

```bash
notepad %APPDATA%\Claude\claude_desktop_config.json
```

### 2. Add Configuration

**Via npm with env vars (recommended):**

```json
{
  "mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": ["--yes", "@daxiong888/mcp-jenkins@latest"],
      "env": {
        "MCP_JENKINS_URL": "https://pipeline.yourcompany.com",
        "MCP_JENKINS_USER": "your_username",
        "MCP_JENKINS_API_TOKEN": "your_api_token"
      }
    }
  }
}
```

**Via npm with a CLI URL arg (credentials stay in env):**

```json
{
  "mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": [
        "--yes",
        "@daxiong888/mcp-jenkins@latest",
        "--url",
        "https://pipeline.yourcompany.com"
      ],
      "env": {
        "MCP_JENKINS_USER": "your_username",
        "MCP_JENKINS_API_TOKEN": "your_api_token"
      }
    }
  }
}
```

**Local installation (credentials stay in env):**

```json
{
  "mcpServers": {
    "jenkins": {
      "command": "node",
      "args": ["/absolute/path/to/mcp-jenkins/dist/index.js"],
      "env": {
        "MCP_JENKINS_URL": "https://pipeline.yourcompany.com",
        "MCP_JENKINS_USER": "your_username",
        "MCP_JENKINS_API_TOKEN": "your_api_token"
      }
    }
  }
}
```

**Bearer token example:**

```json
{
  "mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": ["--yes", "@daxiong888/mcp-jenkins@latest"],
      "env": {
        "MCP_JENKINS_URL": "https://pipeline.yourcompany.com",
        "MCP_JENKINS_BEARER_TOKEN": "your_bearer_token"
      }
    }
  }
}
```

**Multiple Jenkins instances:**

```json
{
  "mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": ["--yes", "@daxiong888/mcp-jenkins@latest"],
      "env": {
        "MCP_JENKINS_URL": "https://pipeline.yourcompany.com,https://scheduler.yourcompany.com",
        "MCP_JENKINS_USER": "your_username,your_username",
        "MCP_JENKINS_API_TOKEN": "token1,token2"
      }
    }
  }
}
```

### 3. Restart

Quit (Cmd+Q / Alt+F4) and reopen Claude Desktop.

## 📝 VSCode

**For:** VSCode with MCP-compatible extensions (Cline, Claude Dev, Continue)

**Cline:**

Settings (Cmd+, / Ctrl+,) → Search "Cline: MCP Settings" → Edit in settings.json:

```json
{
  "cline.mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": ["@daxiong888/mcp-jenkins"],
      "env": {
        "MCP_JENKINS_URL": "https://pipeline.yourcompany.com",
        "MCP_JENKINS_USER": "your_username",
        "MCP_JENKINS_API_TOKEN": "your_api_token"
      }
    }
  }
}
```

**Multiple Jenkins instances:**

```json
{
  "cline.mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": ["@daxiong888/mcp-jenkins"],
      "env": {
        "MCP_JENKINS_URL": "https://pipeline.yourcompany.com,https://scheduler.yourcompany.com",
        "MCP_JENKINS_USER": "your_username,your_username",
        "MCP_JENKINS_API_TOKEN": "token1,token2"
      }
    }
  }
}
```

Or for local installation, use `"command": "node"` and `"args": ["/absolute/path/to/mcp-jenkins/dist/index.js"]`

**Claude Dev / Continue:**

Command Palette (Cmd+Shift+P) → "MCP: Edit Config" → Add similar configuration.

Reload window after configuration.

## 🌐 Cursor

**For:** Cursor IDE with built-in AI

Settings (Cmd+, / Ctrl+,) → Search "MCP" → Edit Config or open `~/.cursor/mcp_config.json`:

```json
{
  "mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": ["--yes", "@daxiong888/mcp-jenkins@latest"],
      "env": {
        "MCP_JENKINS_URL": "https://pipeline.yourcompany.com",
        "MCP_JENKINS_USER": "your_username",
        "MCP_JENKINS_API_TOKEN": "your_api_token"
      }
    }
  }
}
```

**Multiple Jenkins instances:**

```json
{
  "mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": ["--yes", "@daxiong888/mcp-jenkins@latest"],
      "env": {
        "MCP_JENKINS_URL": "https://pipeline.yourcompany.com,https://scheduler.yourcompany.com",
        "MCP_JENKINS_USER": "your_username,your_username",
        "MCP_JENKINS_API_TOKEN": "token1,token2"
      }
    }
  }
}
```

Or for local installation, use `"command": "node"` and `"args": ["/absolute/path/to/mcp-jenkins/dist/index.js"]`

Restart Cursor (Cmd+Q / Alt+F4) after configuration.

## 🌊 Windsurf

**For:** Windsurf AI-powered code editor

Settings → **AI Settings** → **Model Context Protocol** → Add Server:

```json
{
  "mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": ["--yes", "@daxiong888/mcp-jenkins@latest"],
      "env": {
        "MCP_JENKINS_URL": "https://pipeline.yourcompany.com",
        "MCP_JENKINS_USER": "your_username",
        "MCP_JENKINS_API_TOKEN": "your_api_token"
      }
    }
  }
}
```

**Multiple Jenkins instances:**

```json
{
  "mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": ["--yes", "@daxiong888/mcp-jenkins@latest"],
      "env": {
        "MCP_JENKINS_URL": "https://pipeline.yourcompany.com,https://scheduler.yourcompany.com",
        "MCP_JENKINS_USER": "your_username,your_username",
        "MCP_JENKINS_API_TOKEN": "token1,token2"
      }
    }
  }
}
```

Or edit `~/.windsurf/mcp_settings.json` directly. For local installation, use `"command": "node"` with full path.

Restart Windsurf after configuration.

## 🤖 GitHub Copilot

**For:** GitHub Copilot in VSCode or other editors

**⚠️ Not supported.** GitHub Copilot doesn't support MCP servers.

**Options:**

- **In VSCode:** Install Copilot + Cline/Claude Dev. Use Copilot for coding, Cline/Claude Dev for Jenkins (see the VSCode section)
- **In terminal:** Use Copilot for coding suggestions, an MCP-compatible CLI for Jenkins (see the CLI sections above)
- **No MCP:** Use code in `src/lib/jenkins-client.ts` as examples for direct API calls

## 💻 GitHub Copilot CLI

**For:** New GitHub Copilot CLI (`@github/copilot`) in your terminal

**Note:** This is for the NEW Copilot CLI (`npm install -g @github/copilot`), not the old `gh copilot` extension.

### Option 1: Config File with Env Vars (Recommended)

Create or edit `~/.copilot/mcp-config.json`:

```json
{
  "mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": ["--yes", "@daxiong888/mcp-jenkins@latest"],
      "env": {
        "MCP_JENKINS_URL": "https://pipeline.yourcompany.com",
        "MCP_JENKINS_USER": "your_username",
        "MCP_JENKINS_API_TOKEN": "your_api_token"
      }
    }
  }
}
```

Then run: `copilot`

### Option 2: Config File with a CLI URL Arg

Credentials stay in `env`; only the non-secret URL is passed as an argument:

```json
{
  "mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": [
        "--yes",
        "@daxiong888/mcp-jenkins@latest",
        "--url",
        "https://pipeline.yourcompany.com"
      ],
      "env": {
        "MCP_JENKINS_USER": "your_username",
        "MCP_JENKINS_API_TOKEN": "your_api_token"
      }
    }
  }
}
```

### Option 3: Config File Reference

Save the config to a file and reference it (an inline `--additional-mcp-config '{...}'` would land the whole JSON — credentials included — in your shell history):

```bash
# Save config to jenkins-mcp.json (keep it out of version control)
copilot --additional-mcp-config @jenkins-mcp.json
```

For local installation, use `"command": "node"` with the path in args and credentials in `env`:

```json
"args": ["/absolute/path/to/mcp-jenkins/dist/index.js"],
"env": { "MCP_JENKINS_URL": "...", "MCP_JENKINS_USER": "...", "MCP_JENKINS_API_TOKEN": "..." }
```

### Option 4: Multiple Jenkins instances

```json
{
  "mcpServers": {
    "jenkins": {
      "command": "npx",
      "args": ["--yes", "@daxiong888/mcp-jenkins@latest"],
      "env": {
        "MCP_JENKINS_URL": "https://pipeline.yourcompany.com,https://scheduler.yourcompany.com",
        "MCP_JENKINS_USER": "your_username,your_username",
        "MCP_JENKINS_API_TOKEN": "token1,token2"
      }
    }
  }
}
```

### Option 5: Allow All Tools (Non-Interactive)

For scripts and automation:

```bash
copilot --allow-all-tools -p "Check status of mobile-pipeline build"
```

## 🔧 All JetBrains IDEs

**For:** IntelliJ IDEA, WebStorm, PyCharm, GoLand, RubyMine, PhpStorm, Rider, CLion, DataGrip

Settings (Cmd+, / Ctrl+,) → **Tools** → **AI Assistant** → **Model Context Protocol** → Add Server:

- **Name:** Jenkins
- **Command:** `npx`
- **Arguments:** `--yes @daxiong888/mcp-jenkins@latest`
- **Environment Variables:**
  ```
  MCP_JENKINS_URL=https://pipeline.yourcompany.com
  MCP_JENKINS_USER=your_username
  MCP_JENKINS_API_TOKEN=your_api_token
  ```

**Multiple Jenkins instances:** set these environment variables:

```
MCP_JENKINS_URL=https://pipeline.yourcompany.com,https://scheduler.yourcompany.com
MCP_JENKINS_USER=your_username,your_username
MCP_JENKINS_API_TOKEN=token1,token2
```

For local installation, use **Command:** `node` and **Arguments:** `/absolute/path/to/mcp-jenkins/dist/index.js`

Apply and restart the IDE.

**Alternative:** If MCP isn't available, use an MCP-compatible CLI from the built-in terminal (Alt+F12 / ⌥F12)

**Notes:**

- Ultimate/Professional editions: Full AI Assistant MCP support
- Community editions: Use an MCP-compatible CLI from the terminal instead
